import React from 'react';

const NotFoundPage = () => (
  <section className="my-5">
    <h1>Not Found</h1>
    <p>You just discovered a page that does not exist.</p>
  </section>
);

export default NotFoundPage;
